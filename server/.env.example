# Copy to .env (in this directory) and fill in real values. Never commit .env.

PORT=4300
DB_PATH=./data/dev.sqlite

# Xtream provider credentials (encrypted at rest once the credential store lands)
# PROVIDER_BASE_URL=
# PROVIDER_USERNAME=
# PROVIDER_PASSWORD=
